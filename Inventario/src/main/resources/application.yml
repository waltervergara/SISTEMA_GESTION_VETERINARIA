spring:
  application:
    name: inventario

  datasource:
    url: jdbc:mysql://localhost:3306/registro_inventario
    username: root
    password: ""

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    repeatable-sql-migration-prefix: true

  web:
    error:
      include-binding-errors: always
      include-message: always

server:
  port: 8083

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server:8100/eureka/